version: 0.2

phases:
  install:
    - echo Installing dependencies
    - npm install 
  build: 
    commands:
      - npm run build
      - npm run cdk -- synth 

artifacts: 
  base-directory: cdk.out
  files: 
    - '**/*'